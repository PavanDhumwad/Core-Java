package Exception;

public class demoExcepion
{
	public static void main(String[] args) 
	{
		// TODO Auto-generated method stub
		int d;
		int a[]=new int[2];
		
		
		try //try-1
		{ // monitor a block of code.
			System.out.println("This is printed");
			d = 0;
			d = 42 / d;
			System.out.println("This will not be printed.");
		} 
		catch (ArrayIndexOutOfBoundsException e) //catch-1
		{
			System.out.println("In AE");
		}
		catch (Exception e)
		{
			System.out.println("From exception class");
		}

//Since you have already written 'Exception' class you can't write any particular exception class hence below.
//If you do so it will create compile time error(try removing multi line comment below.)
//This is obvious also because 'Exception' class handels everything and if you write any particular exception 
//class hence below it will be reduntant.
		/*catch (ArrayIndexOutOfBoundsException e) 
		{
			System.out.println("In AE");
		}
		*/
/*Every try block should be followed by one more catch statements OR finally block.

 * So you can't write try block followed by another try block.

 * There can be n number of catch statements for a given try block.In such cases if exception is generated by 
	try block then control will inspect every catch statements  in order of their apperance , and the first one
 	whose type matches that of the exception is executed.But if it DOESN'T finds any compatible exception type 
 	prg will be TERMINATED
 	
 */	
		
		System.out.println("After first try-catch statement.");
		
		
		try//try-2
		{
			a[20]=-1;
		}
		//System.out.println("After catch statement.");You can't print @ this line
		//This means after completing try block immediate statement must be catch
		catch (ArithmeticException e) //catch-2
		{ 
			System.out.println("Division by zero.");
		}
//Here catch-2 belongs to try-2 ONLY and not to try-1.Sine both(try-2 & catch-2) are incompatible 
//exception types prg will be TERMINATED immediately		
		
		System.out.println("After second try-catch statement.");
	}
}
